import React from 'react';
import {NavLink} from "react-router-dom";

const TestPage = () => {
   return (
       <NavLink to='/'>
          TestPage
       </NavLink>
   );
};

export default TestPage;
